export type PropType = {
  id: number;
  image: string,
  name: string;
  rating: number,
  star: string,
  reviews: number,
  price: number,
  text?: string,
  amount?: string,
  unit?: string,
  ingimage?: string,
  ingname?: string,
};